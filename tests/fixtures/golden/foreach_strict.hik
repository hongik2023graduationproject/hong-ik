각각 정수 n [1, 2] 에서:
    출력(n)
각각 문자 c "ab" 에서:
    출력(c)
시도:
    각각 정수 x 5 에서:
        출력(x)
실패 오류:
    출력("거부1")
시도:
    각각 문자 k {"k": 1} 에서:
        출력(k)
실패 오류:
    출력("거부2")
시도:
    각각 정수 y "ab" 에서:
        출력(y)
실패 오류:
    출력("거부3")
