클래스 점:
    정수 x
    정수 y
    생성(정수 a, 정수 b):
        자기.x = a
        자기.y = b
    함수 합() -> 정수:
        리턴 자기.x + 자기.y

점 p = 점(3, 4)
출력(p.x)
출력(p.y)
출력(p.합())
점 q = 점(10, 20)
출력(q.x + q.y)
출력(q.합())
