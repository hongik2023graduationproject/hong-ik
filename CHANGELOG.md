# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다. [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/) 형식을 따릅니다.

## [Unreleased] - 2026-06-11

### 추가 (Added)
- **정적 타입 검사기 Phase B-2 (클래스 본문 분석)** — `InstanceType` 도입(부모 체인 서브타이핑: `동물 a = 강아지(...)` 통과), 미지 필드/메서드 접근 진단(`TC201`), 필드 타입·메서드 시그니처 추론, 생성자/메서드 본문 검사(`자기`/`부모` 바인딩, 동적 필드는 첫 대입 타입으로 등록), 생성자 미정의 자식 클래스의 부모 arity 상속(Phase A `TC101` 오탐 수정). `analyzer/type_checker`를 관심사별 3파일로 분리.
- **정적 타입 검사기 Phase B-1 (표현식 정밀화)** — 진단에 정확한 줄 번호(AST `line` 스탬프), 이항 연산자 피연산자 검사(`TC601`)와 결과 타입 정밀 추론, `반복-부터-까지` 범위 타입 검사(`TC301`), `만약 x != 없음 라면:` 분기 내 Optional 좁히기(`&&` 결합·재대입 해제 포함). 진단은 두 런타임(evaluator·VM)이 **모두 거부하는 조합만** 발화하며, 실측에서 발견된 백엔드 불일치 5건은 spec 부록 B에 기록.
- **정적 타입 검사기 Phase A** (`--type-check=off|warn|strict`, 기본 `warn`) — 파서 직후 AST를 검사하는 별도 패스(`analyzer/type_checker`). 런타임(evaluator/VM) 동작 변경 없음.
  - 진단 7종: `TC001`(선언 타입 불일치), `TC002`(재대입 타입 불일치), `TC006`(미선언 식별자), `TC101`(호출 인자 개수), `TC102`(호출 인자 타입), `TC103`(리턴 타입), `TC501`(Optional 미해제 사용).
  - `warn`은 stderr 경고 후 실행 계속, `strict`는 파일 모드에서 진단 발생 시 실행 중단(종료 코드 1). REPL은 항상 warn (strict 요청 시 안내 1회).
  - 빌트인 45종 시그니처 테이블(`object/builtin_signatures.h`) + 고차 함수(`매핑`/`걸러내기`/`줄이기`) 사전 등록.
  - Phase A 범위: 클래스는 이름·생성자 인자 개수만 검사(본문 미진입), 컬렉션 원소 타입·좁히기(narrowing)·이항 연산자 일반 호환성은 Phase B 이후.

### 보안 (Security)
- `opImport`(`가져오기`)의 경로 검증 추가 — `..`, 절대 경로, 드라이브 문자 차단. 인터프리터를 호스팅하는 백엔드의 임의 파일 읽기 위협까지 함께 차단.
- `currentFrame()`의 빈 vector UB 가드 — 손상된 바이트코드 등으로 호출 프레임이 비면 명시적 `RuntimeException`을 던진다.

### 수정 (Fixed)
- **파일 모드 줄 번호 버그** — `Lexer::Tokenize`가 호출마다 줄 번호를 리셋해(REPL 지향 동작) 파일을 줄 단위로 토크나이즈하던 FileMode에서 톱레벨 문장의 줄 번호가 전부 1로 나오던 문제. 파일 전체를 한 번에 토크나이즈하도록 변경 — 런타임 에러의 `[줄 N]` 표기도 이제 정확하다.
- `OP_POW` 정수 오버플로 — `__builtin_mul_overflow` 감지 후 double 경로로 폴백 (signed integer overflow UB 제거).
- void 빌트인의 nullptr 반환을 `Null` 객체로 통일. 호출자(VM, evaluator)의 redundant 삼항 분기 제거.

### 리팩토링 (Refactor)
- `VMValue`의 union 멤버 + tag를 private화하고 inline accessor(`isInt`/`asInt`/`kind` 등)로 캡슐화. 외부에서 활성 union 멤버 mismatch를 만들 수 없게 됨. 향후 `std::variant` 전환은 단일 파일 수정으로 가능.

### 운영/인프라 (Ops)
- `.gitignore`에 `build-wasm/` 추가 + 기존 82개 산출물 untrack.
- MIT LICENSE 추가.
- `SECURITY.md` (취약점 신고 절차 + 알려진 보안 모델).
- `.github/PULL_REQUEST_TEMPLATE.md`, `.env.example` 추가.
- Dependabot (github-actions, weekly).

### 테스트
- VMTest 신규 7건 (path traversal 거부 ×4, `OP_POW` 오버플로 ×3).
- `ReplTest.import*`의 `std::tmpnam` 절대 경로 사용을 상대 경로로 전환 — Windows에서 사전 실패하던 케이스도 해소.

## [0.7.0] - 2026-03-20

### 추가
- 거듭제곱 연산자(`**`) 지원
- 증감 연산자(`++`, `--`) 지원
- VM 단락 평가(short-circuit evaluation) 구현

### 수정
- UTF-8 문자열 처리 개선
- 문자열 이스케이프 시퀀스 수정
- null 역참조, 배열 범위 초과(OOB) 접근 시 안전 처리
- 상속 시 필드 초기화 버그 수정
- 파일 경로 보안 검증 강화

### 테스트
- 테스트 10건 추가

## [0.6.0] - 2026-03-15

### 추가
- `계속`(continue)문 구현
- 람다(Lambda) 지원
- 메서드 체이닝 지원
- VM 동등성(parity) 확대 -- 트리워킹과 VM 모드 간 기능 일치 개선

### 수정
- 보안/안정성 코드 감사 32건 전체 수정

## [0.5.0] - 2026-03-15

### 추가
- 클래스 상속 (`클래스 자식 < 부모`)
- 고차 함수: `매핑`, `걸러내기`, `줄이기`
- 기본 매개변수 지원 (`함수 인사(문자 이름, 문자 접미사 = "님")`)
- 범위 반복문 (`반복 정수 i = 0 부터 10 까지`)
- ForEach 반복문 (`각각 정수 원소 [1,2,3] 에서`)
- 비교/매칭문 (`비교`/`경우`/`기본`)
- 예외 처리 (`시도`/`실패`)
- 모듈 가져오기 (`가져오기 "파일명.hik"`)
- 에러 복구 기능

## [0.4.0] - 2026-03-15

### 추가
- 바이트코드 VM 구현 (`--vm` 플래그)
  - 스택 기반 가상 머신
  - 바이트코드 컴파일러
  - VM 상수 폴딩 최적화
  - VM 클로저 및 REPL 세션 지원
- 수학 내장 함수 11개: `사인`, `코사인`, `탄젠트`, `로그`, `자연로그`, `거듭제곱`, `파이`, `자연수e`, `반올림`, `올림`, `내림`
- JSON 내장 함수 2개: `JSON_파싱`, `JSON_직렬화`
- 문자열 내장 함수 5개: `시작확인`, `끝확인`, `반복`, `채우기`, `부분문자`
- 음수 인덱싱 지원 (`목록[-1]`)
- 튜플 타입 지원
- 문자열 보간 (`"안녕 {이름}님"`)

### 수정
- 트리워킹 파일 모드 수정
- 에러 메시지 한국어화 및 개선 (줄 번호 포함)

## [0.3.0] - 2026-03-15

### 추가
- 언어 대규모 확장 및 문법 개선
  - 사전(Dictionary) 타입
  - 클래스/OOP (`클래스`, `생성`, `자기`)
  - Null 타입 (`없음`)
- 내장 함수 대폭 확대 (28개 -> 46개)
  - 수학: `절대값`, `제곱근`, `최대`, `최소`, `난수`
  - 문자열: `분리`, `대문자`, `소문자`, `치환`, `자르기`
  - 배열: `정렬`, `뒤집기`, `찾기`, `조각`
  - 타입 변환: `정수변환`, `실수변환`, `문자변환`
  - 기타: `타입`, `키목록`, `포함`, `설정`, `삭제`, `파일읽기`, `파일쓰기`

## [0.2.0] - 2026-02-28

### 변경
- raw pointer에서 smart pointer(shared_ptr)로 전환
- 크리티컬 메모리 관련 버그 수정

## [0.1.0] - 2025-07-01 ~ 2025-07-22

### 추가
- 한글 키워드 기반 프로그래밍 언어 기본 구현
  - 변수 선언: `정수`, `실수`, `문자`, `논리`, `배열`
  - 조건문: `만약`/`라면`/`아니면`
  - 반복문: `반복`/`동안`
  - 함수 선언 및 호출: `함수`, `리턴`
  - Boolean 연산자: `&&`, `||`
- 기본 내장 함수: `출력`, `길이`, `추가`, `입력`
- REPL 모드 및 파일 모드 지원
- UTF-8 한글 인코딩 처리
- Google Test 기반 테스트 인프라 구축
  - Lexer, Parser, UTF-8 Converter 테스트

### 수정
- Visual Studio UTF-8 컴파일 옵션 추가
- 구문 미완성 시 nullptr 참조 방지 (NoTokenException)
- 문자열 닫는 따옴표 누락 시 에러 처리

## [0.0.1] - 2024-12-17 ~ 2025-06-30

### 추가
- 프로젝트 초기 생성
- Lexer: 사칙연산, 정수, 식별자, 예약어 토큰화
- Parser: 사칙연산 AST 생성, prefix 연산자
- Evaluator: 정수 사칙연산 실행
- REPL 초안 구현
- `.clang-format` 코드 스타일 설정
